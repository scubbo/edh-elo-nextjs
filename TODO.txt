- [ ] I'm not happy with how the Migrations have been lumped-in with build. This seems to be necessary in order to get
       Vercel's CI/CD to pick them up, but it means that I can't build-in-isolation locally. There must be a better way!
- [ ] Provide alternative game-creation API with primitive single-winner version.
- [ ] Parse data from Google Sheets more robustly. Rows that can't be interpreted are logged and skipped, which is
       invisible on a scheduled run - consider surfacing skipped rows somewhere the group will actually see them.
- [ ] `backCalculateAllEloScores` orders games by date alone, so games sharing a date are replayed in whatever order
       Postgres returns them. Needs a tiebreaker (id?) to make ELO reproducible.